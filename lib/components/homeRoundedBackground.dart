import 'package:flutter/cupertino.dart';

import '../utilis/colors.dart';

class HomeRoundedBackground extends StatelessWidget {
  const HomeRoundedBackground({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [

      ],
    );
  }
}
